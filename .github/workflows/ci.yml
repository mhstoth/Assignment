name: Node.js CI

on:
  push:
    branches: [ "b-master", "develop" ]
  pull_request:
    branches: [ "b-master", "develop" ]

jobs:
  backend-test:
    name: Backend Test & Audit
    runs-on: ubuntu-latest

    services:
      mongodb:
        image: mongo:5.0
        ports:
          - 27017:27017
        options: >-
          --health-cmd mongo
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - uses: actions/checkout@v4
    
    - name: Use Node.js 20.x
      uses: actions/setup-node@v4
      with:
        node-version: 20.x
        cache: 'npm'
        
    - name: Install Dependencies
      run: npm ci
      
    - name: Security Audit
      run: npm audit --audit-level=high

    - name: Run Lint
      run: npm run lint

    - name: Run Tests
      run: npm test
      env:
        DB: mongodb://localhost:27017/test-db
        COOKIE_NAME: placemark-test-cookie
        COOKIE_PASSWORD: secretpasswordforciwhichislongenoughtobasecure32chars
        cloud_name: test_cloud
        api_key: '123456789012345'
        api_secret: test_secret
        JWT_PASSWORD: secretpasswordforciwhichislongenoughtobasecure32chars
        SKIP_IMAGE_TESTS: "true"
        TEST_PORT: 4000
        TEST_HOST: 127.0.0.1
        TEST_LISTEN_HOST: 127.0.0.1
        USE_INJECT: "true"

  frontend-check:
    name: Frontend Check & Build
    runs-on: ubuntu-latest
    env:
      PUBLIC_API_BASE_URL: http://localhost:3000
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Use Node.js 20.x
      uses: actions/setup-node@v4
      with:
        node-version: 20.x
        cache: 'npm'
        cache-dependency-path: client/package-lock.json
        
    - name: Install Dependencies
      working-directory: ./client
      run: npm ci
      
    - name: Svelte Check
      working-directory: ./client
      run: npm run check
      
    - name: Run Lint
      working-directory: ./client
      run: npm run lint
      
    - name: Build
      working-directory: ./client
      run: npm run build
