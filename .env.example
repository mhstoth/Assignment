COOKIE_NAME=discoverRegensburg
COOKIE_PASSWORD=change-me-to-a-long-random-secret

# MongoDB (Atlas connection string incl. database name at the end)
DB=mongodb+srv://<user>:<password>@<cluster>/<database>?retryWrites=true&w=majority

# Cloudinary (used by image upload)
cloud_name=your_cloud_name
api_key=your_api_key
api_secret=your_api_secret
